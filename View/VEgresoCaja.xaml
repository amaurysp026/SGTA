<Window x:Class="SFCH.View.VEgresoCaja"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SFCH.View" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Egreso Efectivo de Caja" Height="500" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBox TextAlignment="Center" FontSize="20" FontWeight="Bold" >Egresos de caja</TextBox>
        <StackPanel HorizontalAlignment="Center" Grid.Row="1">
            <StackPanel  Orientation="Horizontal">
                <TextBox Name="txtnombre" Margin="5" materialDesign:HintAssist.Hint="Entrego a :" Width="250" Style="{StaticResource MaterialDesignOutlinedTextBox}" ></TextBox>
                <Button Height="50" Background="OrangeRed" Click="Button_Click_1" >Cancelar</Button>
            </StackPanel>
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <TextBox Margin="5" Name="txtmonto" materialDesign:HintAssist.Hint="La suma de:" Width="250" Style="{StaticResource MaterialDesignOutlinedTextBox}" ></TextBox>
                <Button Height="50" Click="Button_Click_2" >Registrar</Button>
            </StackPanel>
        </StackPanel>
        <DataGrid Name="data" AutoGenerateColumns="False"  CanUserAddRows="False" Margin="5" Grid.Row="2">
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Header="Numero" Binding="{Binding Id}" Width="auto"/>
                <DataGridTextColumn IsReadOnly="True" Header="Nombre" Binding="{Binding Descripcion}" Width="*"/>
                <DataGridTextColumn IsReadOnly="True" Header="Monto" Binding="{Binding Egreso}" Width="auto"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Margin="5" HorizontalAlignment="Center" Grid.Row="3">
            <TextBlock FontSize="20" FontWeight="Bold" >MONTO TOTAL RETIRADO</TextBlock>
            <Separator/>
            <TextBlock HorizontalAlignment="Center" Name="txttotal" FontSize="20" FontWeight="Bold" > RD$ 0.00 </TextBlock>
            <Button Height="50" Margin="5" Click="Button_Click" >Salir</Button>
        </StackPanel>
    </Grid>
</Window>
