<Page x:Class="SFCH.View.PCuentas"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:SFCH.View"  xmlns:dgx="clr-namespace:DataGridExtensions;assembly=DataGridExtensions"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PCuentas">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition ></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition x:Name="cold" Width="auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Height="50" Visibility="Collapsed" Margin="3" HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="btnRecalcular" Click="btnRecalcular_Click" >
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" >
                    <materialDesign:PackIcon HorizontalContentAlignment="Center" VerticalAlignment="Center" VerticalContentAlignment="Center" Kind="CalculatorVariant" Width="30" Height="30"></materialDesign:PackIcon>
                    <TextBlock TextWrapping="Wrap" >Recalcular Cuentas</TextBlock>
                </StackPanel>
            </Button>
            <Button Height="50" Margin="3" HorizontalAlignment="Center" Click="Button_Click">
                <StackPanel>
                    
                    <TextBlock>Reporte de cuentas</TextBlock>
                </StackPanel>
            </Button>
            <Button Height="50" Margin="3" HorizontalAlignment="Center" Click="Button_Click_1">
                <StackPanel>

                    <TextBlock>Reporte de cuentas detallado</TextBlock>
                </StackPanel>
            </Button>
        </StackPanel>
        <DataGrid Grid.Row="1" Margin="5" SelectionChanged="data_SelectionChanged" IsReadOnly="True" AutoGenerateColumns="False" x:Name="data" dgx:DataGridFilter.IsAutoFilterEnabled="True" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Numero de Cuenta" Binding="{Binding NumeroCuenta}" Width="auto"/>
                <DataGridTextColumn Header="Titular" Binding="{Binding Titular}" Width="auto"/>
                <DataGridTextColumn Header="Saldo" Binding="{Binding Saldo, StringFormat='{}{0:C}'}" Width="auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Interes" Binding="{Binding Interes, StringFormat='{}{0:C}'}" Width="auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Right"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Fecha de Apertura" Binding="{Binding FechaApertura,StringFormat='dd/MM/yyyy'}" Width="auto"/>
                <DataGridTextColumn Header="Tipo de cuenta" Binding="{Binding TipoCuenta}" Width="auto"/>
                <DataGridTextColumn Header="Descripcion" Binding="{Binding NombreCuenta}" Width="auto"/>
                <DataGridTextColumn Header="Moneda" Binding="{Binding Moneda}" Width="auto"/>
               
               
            </DataGrid.Columns>
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Ver Transacciones" Click="MenuItem_Click"/>
                    <MenuItem Header="Desactivar Cuenta" Click="MenuItem_Click_1"/>
                    <MenuItem Header="Recualcular esta cuenta" Click="MenuItem_Click_2"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
        <DataGrid x:Name="data2" IsReadOnly="True" Visibility="Collapsed" Margin="5" Grid.Column="1">
            
        </DataGrid>
        <TextBlock Grid.Row="2" FontSize="16" x:Name="txttotal">Monto Total en cuenta: </TextBlock>
    </Grid>
</Page>
