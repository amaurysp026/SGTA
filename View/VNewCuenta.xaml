<Window x:Class="SFCH.View.VNewCuenta"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SFCH.View" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Apertura de Nueva Cuenta" Height="450" Width="800" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="40" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition Width="AUTO" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5in"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="0.8in"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Name="txtTitulo" Grid.Column="0" Grid.ColumnSpan="3" TextAlignment="Center" FontWeight="Bold" FontSize="30" HorizontalAlignment="Stretch"  ></TextBlock>
        <ComboBox Grid.Row="1" Name="cbTCuenta" materialDesign:HintAssist.Hint="Tipo de cuenta" SelectedIndex="0" SelectedItem="{Binding TipoCuenta}" Margin="4"  Style="{StaticResource MaterialDesignOutlinedComboBox}" >
        </ComboBox>
        <ComboBox Grid.Row="1" Grid.Column="1" Name="cbNomCuenta" materialDesign:HintAssist.Hint="Nombre de Cuenta" SelectedIndex="0" Text="{Binding NombreCuenta}" Margin="4"  Style="{StaticResource MaterialDesignOutlinedComboBox}" >
            <ComboBox.Items>
                <TextBlock>APORTE A CAPITAL COOPGAFAR</TextBlock>
                <TextBlock>PERSONAL</TextBlock>
            </ComboBox.Items>
        </ComboBox>
        <Grid Grid.Row="2" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <ComboBox  Name="cbTtitular" materialDesign:HintAssist.Hint="Tipo de Entidad" SelectedIndex="0" Text="{Binding TipoIdentificacion}" Margin="4"  Style="{StaticResource MaterialDesignOutlinedComboBox}" >
            </ComboBox>
            <Button Margin="4" Grid.Column="1" Height="50" Click="Button_Click_2" >Buscar</Button>

        </Grid>
        <TextBox CharacterCasing="Upper" Grid.Row="2" Name="txtNombre" Grid.Column="0" Margin="4" materialDesign:HintAssist.Hint="Nombre" Text="{Binding Titular.Nombre,Mode=TwoWay}"  Style="{StaticResource MaterialDesignOutlinedTextBox}"></TextBox>

      
        <TextBox Visibility="Hidden" CharacterCasing="Upper"  Grid.Row="4" Grid.Column="0" Margin="4" materialDesign:HintAssist.Hint="Telefono" Text="{Binding Telefono}" Style="{StaticResource MaterialDesignOutlinedTextBox}"></TextBox>
        <TextBox Visibility="Hidden" CharacterCasing="Upper"  Grid.Row="4" Grid.Column="1" Margin="4" materialDesign:HintAssist.Hint="Celular" Text="{Binding Celular}"  Style="{StaticResource MaterialDesignOutlinedTextBox}"></TextBox>


        <Button Grid.Row="6" Height="50" Margin="5" Click="Button_Click">
            Guardar
        </Button>
        <Button Background="red" Grid.Row="6" Grid.Column="1" Height="50" Margin="5" Click="Button_Click_1">
            Cancelar
        </Button>
    </Grid>
</Window>
