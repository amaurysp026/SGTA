<Page x:Class="SFCH.View.PComprasList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SFCH.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PComprasList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/> 
        </Grid.RowDefinitions>
        <TextBlock TextAlignment="Center" FontSize="20" Margin="5" >Compras</TextBlock>
        <DataGrid Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" x:Name="data" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Numero" Binding="{Binding NumeroCompra}" Width="auto"/>
                <DataGridTextColumn Header="Numero" Binding="{Binding FechaEmision,StringFormat='dd/mm/yyyy'}" />
                <DataGridTextColumn Header="Proveedor" Binding="{Binding Proveedor}" Width="*"  />
                <DataGridTextColumn Header="Tasa Cambio" Binding="{Binding TasaCambio,StringFormat=C}"/>
                <DataGridTextColumn Header="Total RD$" Binding="{Binding Total,StringFormat=C}"/>
                <DataGridTextColumn Header="Total USD$" Binding="{Binding TotalUSD ,StringFormat=C}"/>
                <DataGridTextColumn Header="Monto Pendiente" Binding="{Binding MontoPendiente,StringFormat=C}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
