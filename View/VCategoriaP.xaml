<Window x:Class="SFCH.View.VCategoriaP"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SFCH.View" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Categorias de Productos" Height="450" Width="400" ResizeMode="NoResize" WindowState="Normal" WindowStartupLocation="CenterScreen">
    <Grid Margin="5" >
        <Grid.RowDefinitions>
            <RowDefinition Height="0.3*"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock FontSize="14" Margin="10" FontWeight="DemiBold" TextAlignment="Center" Grid.ColumnSpan="2" >Categorias de Productos</TextBlock>
            <TextBox Name="txtcat" materialDesign:HintAssist.Hint="Nombre Categoria" Style="{StaticResource MaterialDesignOutlinedTextBox}" Grid.Row="1" Text="" ></TextBox>
            <Button Grid.Row="1" Margin="5" Height="40"  Grid.Column="1" Click="Button_Click">Guardar</Button>
        </Grid>
        <DataGrid SelectionChanged="data_SelectionChanged" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Name="data" Grid.Row="1" >

            <DataGrid.ContextMenu>
                <ContextMenu >
                    <MenuItem Click="MenuItem_Click" Header="Desactivar Categoria">
                        <MenuItem.Icon>
                            <materialDesign:PackIcon Kind="NullOff"/>
                        </MenuItem.Icon>
                        
                    </MenuItem>
                </ContextMenu>
            </DataGrid.ContextMenu>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"></DataGridTextColumn>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}"></DataGridTextColumn>
                <DataGridTextColumn Header="Productos" Binding="{Binding CantProducto}"></DataGridTextColumn>
                <DataGridCheckBoxColumn Header="Activa" Binding="{Binding Activo}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
