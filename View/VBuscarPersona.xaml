<Window x:Class="SFCH.View.VBuscarPersona"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SFCH.View" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Buscar Persona" Height="450" Width="800">
    <Grid Margin="15" >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <TextBox Margin="5"  Width="250" materialDesign:HintAssist.Hint="Nombre " Style="{StaticResource MaterialDesignOutlinedTextBox}" >

            </TextBox>
            <Button Height="50" Margin="5" >Buscar</Button>
            <Button Click="Button_Click" Height="50" Margin="5" >Nuevo Cliente</Button>
        </StackPanel>
        <DataGrid x:Name="data" Grid.Row="1" IsReadOnly="True" MouseDoubleClick="data_MouseDoubleClick" >
            
        </DataGrid>
    </Grid>
</Window>
