<Page x:Class="SFCH.View.Precepciones"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SFCH.View"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"    xmlns:mpvk="http://schemas.mpcoding.com/mpcoding/wpf/virtualkeys" xmlns:dgx="clr-namespace:DataGridExtensions;assembly=DataGridExtensions"
     
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Precepciones">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock TextAlignment="Center" >Recepciones</TextBlock>
        <TextBlock Grid.Column="1" TextAlignment="Center" >Receps</TextBlock>
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <DatePicker Margin="5" Name="dap" FontSize="20" materialDesign:HintAssist.Hint="Selecionar Fecha" Style="{StaticResource MaterialDesignOutlinedDatePicker}"></DatePicker>
            <ComboBox Margin="5" Name="cbtanda" materialDesign:HintAssist.Hint="Jornada" FontSize="20" Style="{StaticResource MaterialDesignOutlinedComboBox}" Grid.Column="1" >
                <ComboBoxItem>
                    <TextBlock>Mañana</TextBlock>
                </ComboBoxItem>
                <ComboBoxItem>
                    <TextBlock>Tarde</TextBlock>
                </ComboBoxItem>
                <ComboBoxItem>
                    <TextBlock>Noche</TextBlock>
                </ComboBoxItem>
            </ComboBox>
            <Button Height="50" Margin="5" Grid.Column="2" Name="btnnewrecepcion" Click="btnnewrecepcion_Click" >Crear Nueva Recepción</Button>
           
            
                <StackPanel  Margin="5" Grid.Row="1" Grid.RowSpan="2" Grid.Column="0" Grid.ColumnSpan="2" >
                    <Expander
    HorizontalAlignment="Stretch"
    Header="Mostrar Detalles">
                        <StackPanel
      Margin="24,8,24,16"
      Orientation="Vertical"
      TextBlock.Foreground="{DynamicResource MaterialDesignBody}">
                            <TextBlock
        Text="Your Content" />
                            <TextBlock Text="gjghhghjhghhgh"
        />
                        <ListView>
                            <ListViewItem>
                                <TextBlock>asdasdas</TextBlock>
                            </ListViewItem>
                        </ListView>
                        </StackPanel>
                            
                    </Expander>
                
                </StackPanel>
            
        </Grid>
        <DataGrid Grid.Row="1" x:Name="data" AutoGenerateColumns="False" dgx:DataGridFilter.IsAutoFilterEnabled="True" CanUserAddRows="False" IsReadOnly="True" >
            <DataGrid.ContextMenu>

                <ContextMenu>
                    <MenuItem Header="Agregar Nota"/>
                    <MenuItem Header="Modificar" />
                    <MenuItem Header="Anular" />
                </ContextMenu>
                </DataGrid.ContextMenu>
                    <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto"/>
                <DataGridTextColumn Header="Jornada" Binding="{Binding Tanda}" Width="Auto"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha,StringFormat='{}dd/MM/yyyy'}" Width="auto"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="auto"/>
                <DataGridCheckBoxColumn Header="Pagada" Binding="{Binding Pagada}" Width="auto"/>
                <DataGridTextColumn Header="Nota" Binding="{Binding Observacion}" Width="Auto"/>
            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</Page>
